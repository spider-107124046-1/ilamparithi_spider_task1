# Valid .env-style key=value pairs
APP_ENV=production
DB_HOST=localhost
DB_PORT=5432
FEATURE_FLAG=true
CACHE_TTL=3600
DEBUG=true
# Invalid syntax
# Syntactically valid, but risky
# Unnecessary Quotations
